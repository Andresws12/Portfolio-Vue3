// TODO: Pendient to review use .ts
import viewsJSON from './views.json';
import commonJSON from './common.json';

const views = viewsJSON.views;
const common = commonJSON.common;

const en = {
  views,
  common,
};

export default en;
